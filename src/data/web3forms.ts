/**
 * Access key public của Web3Forms (an toàn để lộ client-side theo thiết kế của
 * Web3Forms — đây KHÔNG phải secret, nhưng cũng không tự chống spam, xem honeypot
 * trong ContactForm). Lấy tại https://web3forms.com sau khi VNQ nhập email xác
 * nhận. Nếu để rỗng, ContactForm báo lỗi cấu hình ngay khi submit thay vì gọi API.
 */
export const WEB3FORMS_ACCESS_KEY = "";
